FROM python:3.12-slim

WORKDIR /workspace

RUN pip install --no-cache-dir \
    "setuptools>=68" \
    wheel \
    pytest \
    typer \
    pydantic \
    pyyaml

ENV PYTHONPATH=/workspace

CMD ["python", "-m", "pytest", "-q"]
